// stb_truetype — delegates to ImGui's bundled copy (imstb_truetype.h).
// The single implementation unit is vendor/stb/stb_truetype.cpp.
// IMGUI_ENABLE_STB_TRUETYPE is intentionally left OFF in imconfig.h, so ImGui
// does not compile the implementation — we own it here.
#pragma once
#include "../imgui/imstb_truetype.h"
