#pragma once
// Auto-generated by marble_configure_executable() — do not edit.
// Include this file ONLY in main.cpp, before EntryPoint.h.
#include <Core/Application.h>

namespace GameMetadata {

    // Passed directly to the Application constructor.
    // All window / render settings live here — change them in CMakeLists.txt.
    inline constexpr Marble::WindowSpec Window {
        .Title        = "@MARBLE_PRODUCT_NAME@",
        .Width        = @MARBLE_WINDOW_WIDTH@,
        .Height       = @MARBLE_WINDOW_HEIGHT@,
        .VSync        = @MARBLE_VSYNC_BOOL@,
        .Fullscreen   = @MARBLE_FULLSCREEN_BOOL@,
        .RenderWidth  = @MARBLE_RENDER_WIDTH@,
        .RenderHeight = @MARBLE_RENDER_HEIGHT@,
        .Style        = Marble::RenderStyle::@MARBLE_RENDER_STYLE@,
    };

    // Runtime window icon path (PNG), relative to the executable.
    // Empty = no icon set at runtime (OS default is used).
    inline constexpr const char* IconPath = "@MARBLE_ICON_PNG_PATH@";

    // Informational — mirrors what is embedded in the .exe resource.
    inline constexpr const char* ProductName = "@MARBLE_PRODUCT_NAME@";
    inline constexpr const char* Version     = "@MARBLE_VERSION_STR@";
    inline constexpr const char* CompanyName = "@MARBLE_COMPANY_NAME@";
    inline constexpr const char* Copyright   = "@MARBLE_COPYRIGHT@";

} // namespace GameMetadata
